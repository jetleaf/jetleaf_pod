name: jetleaf_pod
description: The core module that dependency injection via the pod module.
version: 1.0.0

homepage: https://jetleaf.hapnium.com
repository: https://github.com/jetleaf/jetleaf_pod

topics:
  - ioc
  - dependency-injection
  - service-locator
  - pod-management
  - configuration

keywords:
  - ioc
  - dependency-injection
  - service-locator
  - pod-management
  - configuration

environment:
  sdk: ^3.9.0

dependencies:
  meta: ^1.17.0
  jetleaf_lang: ^1.0.0
  jetleaf_logging: ^1.0.0
  jetleaf_convert: ^1.0.0
  jetleaf_utils: ^1.0.0
  jetleaf_env: ^1.0.0

dev_dependencies:
  test: ^1.21.0
  lints: ^6.0.0